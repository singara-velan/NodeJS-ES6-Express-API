//base service route
const apiServiceRoute = '/api';

const sampleController = {
    routeName: '/sample',
    routeMethods: {
        sampleGet: '/sampleGet'
    }
};

module.exports = {
    apiServiceRoute,
    sampleController
}